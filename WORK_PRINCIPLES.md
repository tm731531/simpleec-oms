# SimpleEC OMS 工作約法三章

**確認日期**: Feb 24, 2026
**確認人**: Tom & Claude

---

## 🔒 三大工作原則

### 1️⃣ 基礎架構不動條款

**原則**: Infrastructure 層的架構和運作方式不輕易變動

**包含範圍**（不可隨意改動）：
- Docker 容器配置和網絡設定
- PostgreSQL 數據庫 schema 和連接配置
- Redis 緩存策略
- Kafka broker 配置和 KRaft 模式
- 系統啟動和重啟機制（systemd、cron）
- Nginx 反向代理基本配置

**允許的改動**：
- 日誌級別調整
- 資源限制優化（memory、CPU）
- 清理策略調整（retention、cleanup）
- 監控和告警規則

**為什麼**：
- Infrastructure 是整個系統的基礎
- 任何改動都會影響所有服務
- 變動必須經過充分測試和回滾計畫

---

### 2️⃣ 跨層安全性一致條款

**原則**: Admin App、User App、Nginx、API Gateway 的安全性必須同步考量，但**僅限於此範圍**

**涵蓋的層級**：
```
User Browser
    ↓
Nginx (port 8089)
    ├─ Admin App (port 8084)
    ├─ User App (port 5173)
    └─ API Gateway (port 8080→8082)
```

**同步考量的安全項目**：
- ✅ JWT 認證和授權機制
- ✅ CORS 策略
- ✅ 會話管理
- ✅ SSL/TLS 配置
- ✅ 請求驗證和速率限制

**不在此範圍內**（各自獨立）：
- ❌ Backend Job 的內部邏輯
- ❌ Kafka 消息的加密
- ❌ 數據庫連接安全
- ❌ 服務間通信協議

**為什麼**：
- 用戶接觸的層級必須一致
- 但深層系統有自己的安全邊界
- 過度耦合會增加複雜度

---

### 3️⃣ 事件流完整性條款

**原則**: Kafka 事件流是一個完整的上下游系統，任何改動都必須考慮全景

**事件流包含的完整鏈路**：
```
Producer (API, Scheduler, Channel Job)
    ↓
Kafka Topic (固定的 schema)
    ↓
Consumer (各個 Job: order-job, backend-job 等)
    ↓
Database Update & Business Logic
    ↓
下一個 Topic 的 Producer
```

**必須保持同步的元素**：
- 📌 Topic 名稱和 Partition 策略
- 📌 Message Schema 和版本控制
- 📌 Producer 發送邏輯
- 📌 Consumer Group 配置
- 📌 Handler 實現邏輯
- 📌 DLT（死信隊列）和重試策略
- 📌 Retention 策略

**改動前的檢查清單**：
- [ ] 確認涉及的所有 Topics
- [ ] 確認所有 Producers 和 Consumers
- [ ] 驗證 Message 結構對齊
- [ ] 確認 Handler 邏輯相應調整
- [ ] 測試完整的消息流轉
- [ ] 驗證監控告警

**為什麼**：
- Kafka 是系統的神經中樞
- 一個環節的問題會影響整個流程
- 沒有人知道消息去了哪裡是致命的

---

## 📋 影響範圍評估表

在任何改動之前，問以下問題：

| 問題 | 答案 | 影響 |
|------|------|------|
| 是否改動了 Infrastructure？ | YES → 停止 | 🔴 違反原則1 |
| 是否改動了 3 個應用層的安全機制？ | 不全部 → 停止 | 🔴 違反原則2 |
| 是否改動了 Kafka 的 Producer/Consumer/Handler？ | YES → 檢查全景 | 🟡 需要完整驗證 |
| 是否影響了未計劃改動的模塊？ | YES → 停止 | 🔴 超出範圍 |

---

## ✅ 工作流程

### 改動前
1. **評估範圍** — 使用上面的表格
2. **確認邊界** — 清楚地列出會改動什麼、不會改動什麼
3. **檢查依賴** — 確認沒有隱藏的依賴關係
4. **告知 Claude** — 明確說明改動計劃

### 改動中
1. **隔離測試** — 在隔離環境測試
2. **持續驗證** — 實時檢查日誌和指標
3. **文檔更新** — 同步更新相關文檔

### 改動後
1. **完整驗證** — 運行完整的端到端測試
2. **監控告警** — 檢查是否有新的警告
3. **更新 OPERATIONS_CURRENT_STATUS.md** — 記錄所有改動

---

## 🚫 明確禁止事項

❌ **絕對禁止**：
1. 改動 Infrastructure 層而不通知
2. 改動安全機制而不在 3 層同步
3. 改動 Kafka 流程而不驗證整個鏈路
4. 改動代碼而不檢查是否影響其他模塊
5. 推送代碼而不更新相關文檔

❌ **高風險操作**（需要充分論證）：
1. 修改 Docker Compose 依賴關係
2. 改變 Topic 的 Partition 或 Retention 策略
3. 修改 Message Schema（需要版本控制）
4. 改動 Nginx 路由規則
5. 修改 JWT 認證邏輯

---

## 📊 違反原則的後果

| 違反級別 | 後果 | 例子 |
|---------|------|------|
| 🟢 輕微 | 要求修正 + 更新文檔 | 日誌級別調整 |
| 🟡 中等 | 回滾 + 重新評估 + 通知 | 改動 CORS 策略 |
| 🔴 嚴重 | 緊急回滾 + 事後分析 | 改動 Docker 配置未通知 |

---

## 💬 溝通方式

在每次開始工作前，Claude 應該：

1. **明確說明改動範圍**
   ```
   本次改動涉及：
   - ✅ [列出會改動的]
   - ❌ [列出不會改動的]
   - ⚠️ [列出需要驗證的]
   ```

2. **檢查依賴關係**
   ```
   依賴檢查：
   - Kafka Topics: [list]
   - Affected Consumers: [list]
   - Security Impact: [describe]
   ```

3. **獲得確認**
   ```
   是否同意按上述範圍進行改動？
   ```

---

## 📝 例子

### ✅ 好的改動計劃
```
任務：修復 API 健康檢查端點

範圍：
- ✅ 修改 HealthController.java
- ✅ 更新 OPERATIONS_CURRENT_STATUS.md
- ❌ 不改動 Docker 配置
- ❌ 不改動 Kafka 配置
- ❌ 不影響其他控制器

驗證：
- [ ] 本地測試 API
- [ ] 檢查日誌無錯誤
- [ ] 驗證 Nginx 代理正常
```

### ❌ 不好的改動計劃
```
任務：優化系統性能

範圍：
- 改動 Docker 配置
- 改動 Kafka 消息結構
- 可能影響其他服務
（模糊且超出範圍）
```

---

## 簽名確認

**Tom** 確認同意以上三大原則 ✓
**Claude** 確認遵守以上三大原則 ✓
- 改進調試方式：先讀代碼、系統檢查數據流，而不是猜測環境問題
- 改動前明確說明範圍，改動後完整驗證
- 禁止事項：Infrastructure 不通知、安全機制不同步、Kafka 不驗證全景、代碼改動不檢查影響

**初始確認日期**: Feb 24, 2026 01:15 AM GMT+8
**Claude 正式承諾**: Feb 24, 2026 01:25 AM GMT+8

---

## 版本歷史

| 版本 | 日期 | 更新內容 |
|------|------|---------|
| 1.0 | Feb 24, 2026 | 初始版本 - 約法三章 |

