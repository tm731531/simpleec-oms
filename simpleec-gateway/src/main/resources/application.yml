spring:
  application:
    name: simpleec-gateway
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8088

webhook:
  secrets:
    shopify: ${WEBHOOK_SECRET_SHOPIFY:}
    shopee: ${WEBHOOK_SECRET_SHOPEE:}
    easystore: ${WEBHOOK_SECRET_EASYSTORE:}

logging:
  level:
    com.simpleec: DEBUG
