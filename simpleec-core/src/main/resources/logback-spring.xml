<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="unknown"/>

    <!-- Console: human-readable for local dev -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [traceId=%X{trace_id:-},msgId=%X{messageId:-},merchant=%X{merchantId:-},action=%X{taskAction:-}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- JSON: structured for Docker / Loki -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <includeMdcKeyName>messageId</includeMdcKeyName>
            <includeMdcKeyName>merchantId</includeMdcKeyName>
            <includeMdcKeyName>taskAction</includeMdcKeyName>
            <includeMdcKeyName>taskType</includeMdcKeyName>
            <includeMdcKeyName>sourceJobType</includeMdcKeyName>
            <includeMdcKeyName>businessTraceId</includeMdcKeyName>
            <customFields>{"service":"${appName}"}</customFields>
        </encoder>
    </appender>

    <!-- Profile-based selection -->
    <springProfile name="docker,production">
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
    <springProfile name="!docker &amp; !production">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- Package-level overrides -->
    <logger name="com.simpleec" level="DEBUG"/>
    <logger name="org.springframework.kafka" level="INFO"/>
    <logger name="org.apache.kafka" level="WARN"/>
</configuration>
